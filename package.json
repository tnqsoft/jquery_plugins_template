{
  "name": "demo1",
  "version": "1.0.0",
  "description": "jQuery Plugin Demo 1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "preparedemo": "cpx demo/**.html dist && cpx demo/**.ico dist",
    "build": "npm run compile && npm run preparedemo"
  },
  "author": "Nguyen Nhu Tuan",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.2.1"
  },
  "devDependencies": {
    "@types/jquery": "^3.2.11",
    "@types/node": "^8.0.22",
    "cpx": "^1.5.0",
    "typescript": "^2.4.2"
  }
}
